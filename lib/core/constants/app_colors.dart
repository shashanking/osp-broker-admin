import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary =
      Color(0xFFFFD700); // Gold color from the screenshot
  static const Color accent =
      Color(0xFFFFC107); // Slightly darker gold for accents

  // Background colors
  static const Color background = Color(0xFF333333);
  static const Color backgroundDark = Color(0xFF121212);
  static const Color surface = Color(0xFF1E1E1E);
  static const Color surfaceDark = Color(0xFF1E1E1E);
  static const Color surfaceLight = Color(0xFF2D2D2D);

  // Text colors
  static const Color textPrimary = Colors.black;
  static const Color textSecondary = Color(0xFF37303b);
  static const Color textDisabled = Color(0xFF6D6D6D);

  // Status colors
  static const Color success = Color(0xFF4CAF50);
  static const Color successLight = Color(0xFFE8F5E9);
  static const Color error = Color(0xFFE53935);
  static const Color errorLight = Color(0xFFEF9A9A);
  static const Color warning = Color(0xFFFFA000);
  static const Color info = Color(0xFF2196F3);

  // Additional UI colors
  static const Color backgroundLight = Color(0xFFF9F6EE);
  static const Color textHint = Color(0xFF9E9E9E);

  // Sidebar colors
  static const Color sidebarBackground = Color(0xFF1A1A1A);
  static const Color sidebarSelected =
      Color(0xFFD49823); // Gold with 20% opacity
  static const Color sidebarHover = Color(0x1AFFFFFF); // White with 10% opacity

  // Divider colors
  static const Color divider = Color(0x1FFFFFFF);
}
